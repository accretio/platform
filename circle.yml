general:
  build_dir: lab

checkout:
  post:
    - echo "export default function setDevConfig(config) {}" > config_dev.js

deployment:
  staging:
    branch: master
    commands:
      - npm run dockerize
