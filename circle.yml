general:
  build_dir: lab

machine:
  environment:
    PROJECT_NAME: accretio-platform
    CLUSTER_NAME: platform-staging
    CLOUDSDK_COMPUTE_ZONE: us-central1-c

dependencies:
  pre:
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update --version 120.0.0
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet components update --version 120.0.0 kubectl
    - echo $ACCT_AUTH | base64 --decode -i > ${HOME}/account-auth.json
    - sudo /opt/google-cloud-sdk/bin/gcloud auth activate-service-account --key-file ${HOME}/gcloud-service-key.json
    - sudo /opt/google-cloud-sdk/bin/gcloud config set project $PROJECT_NAME
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet config set container/cluster $CLUSTER_NAME
    - sudo /opt/google-cloud-sdk/bin/gcloud config set compute/zone ${CLOUDSDK_COMPUTE_ZONE}
    - sudo /opt/google-cloud-sdk/bin/gcloud --quiet container clusters get-credentials $CLUSTER_NAME
  
checkout:
  post:
    - echo "export default function setDevConfig(config) {}" > config_dev.js

test:
  override:
    - "true"
    
deployment:
  staging:
    branch: dockerize-lab
    commands:
      - docker build -t gcr.io/${PROJECT_NAME}/lab:$CIRCLE_SHA1 .
      - docker tag gcr.io/${PROJECT_NAME}/lab:$CIRCLE_SHA1
      - sudo /opt/google-cloud-sdk/bin/gcloud docker -- push gcr.io/${PROJECT_NAME}/lab
